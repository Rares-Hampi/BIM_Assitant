# Development Dockerfile for Frontend with hot-reload
FROM node:20-alpine

WORKDIR /app

# Expose port
EXPOSE 5173

# Start script that installs dependencies then runs Vite
CMD sh -c "npm install && npm run dev -- --host 0.0.0.0"
